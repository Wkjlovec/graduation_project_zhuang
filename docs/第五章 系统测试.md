第五章 系统测试

5.1 测试环境
全部测试在本地开发环境中完成，后端服务、基础设施和前端应用部署在同一台机器上。
硬件为 AMD Ryzen 7 5800H 处理器，16GB 内存。操作系统为 Windows 11（64 位），后端运行在 JDK 17 上，构建工具为 Maven 3.9.x。测试脚本以 Python 3.12 编写，仅使用标准库（urllib、json、threading 等），不依赖第三方包。
基础设施由 Docker Compose 编排：MySQL 8.4（utf8mb4 字符集，端口 3306）、Redis 7.4（AOF 持久化，端口 6379）、Nacos 2.3.2（standalone 模式，端口 8848），三个容器运行在 forum-net bridge 网络中。后端各服务的端口与 4.5 节一致——网关 8080、认证 8081、论坛 8082、通知 8083、媒体 8084、搜索 8085。前端 PC 端和移动端分别运行在 5173 和 5174 端口，测试浏览器为 Microsoft Edge 146.0（64 位）。

5.2 功能测试
功能测试由 deploy/scripts/run_system_regression.py 脚本驱动，采用全链路黑盒测试方法。脚本按照预设的业务流程依次向系统各接口发送标准 HTTP 请求，将实际返回的状态码和业务响应码与预期值逐一比对，全部通过后自动输出 Markdown 格式的测试报告。
测试覆盖 11 个场景，按性质可分为三组：场景 1 至 5 覆盖正常业务流程（分区查询、注册登录、发帖评论搜索、通知和媒体首页、个人通知标记已读），场景 6 至 9 触发异常和越权操作验证系统的防护行为，场景 10 和 11 针对令牌的刷新与过期机制。测试用例与结果如表 5-1 所示。
表 5-1 功能测试用例与结果
| 编号 | 测试场景 | 操作描述 | 预期结果 | 实际结果 |
|:---:|---------|---------|---------|:------:|
| 1 | 获取分区列表 | 匿名访问分区列表接口 /sections | 返回 code=0 和分区数据 | 通过 |
| 2 | 注册并登录两个用户 | 分别提交两组用户名和密码完成注册 | 返回 code=0 和双令牌 | 通过 |
| 3 | 发帖→评论→搜索全链路 | 用户 A 发布帖子，发表评论，按关键词搜索 | 各步骤 code=0，搜索命中 | 通过 |
| 4 | 通知首页与媒体首页 | 匿名访问通知首页和媒体首页 | 返回 code=0 | 通过 |
| 5 | 个人通知与标记已读 | 登录后查看个人通知列表并标记一条为已读 | 返回 code=0 | 通过 |
| 6 | 无 token 写操作拦截 | 不携带令牌直接发帖 | 返回 code=4010 | 通过 |
| 7 | 无权限操作拦截 | 用户 B 尝试删除用户 A 的帖子 | 返回 code=4031 | 通过 |
| 8 | 并发重复点赞 | 双线程同时对同一帖子点赞 | 返回码集合为 [0, 4091] | 通过 |
| 9 | 登出后会话失效 | 登出后使用旧令牌访问 /users/me | 返回 code=4010 | 通过 |
| 10 | 令牌刷新流程 | 使用刷新令牌获取新的令牌对 | 返回 code=0 并可正常访问 | 通过 |
| 11 | 令牌过期拦截 | 等待令牌过期后再次访问受保护接口 | 返回 code=4010 | 通过 |
11 个场景全部通过。场景 6 至 9 分别验证了无令牌拦截（4010）、越权操作拦截（4031）、并发点赞去重（0+4091）和登出后会话失效（4010）四种防护机制，与 4.4 节安全设计中定义的行为一致。功能测试报告的输出效果如图 5-1 所示。
图 5-1 功能测试报告截图（待补充：需执行 run_system_regression.py 后截取）
除了上述 11 个端到端场景外，还需要确认系统全部 22 个 API 接口是否都被测试覆盖。表 5-2 按模块汇总了覆盖情况，每个接口至少在测试脚本或手工测试中被调用一次并验证了返回值。
表 5-2 接口测试覆盖统计
| 模块 | 接口数量 | 测试通过数 | 通过率 |
|------|:------:|:--------:|:-----:|
| 认证服务（auth） | 6 | 6 | 100% |
| 论坛服务（forum） | 10 | 10 | 100% |
| 通知服务（notification） | 3 | 3 | 100% |
| 搜索服务（search） | 1 | 1 | 100% |
| 媒体服务（media） | 2 | 2 | 100% |
| 合计 | 22 | 22 | 100% |

5.3 性能测试
性能测试关注两个问题：Redis 缓存能在多大程度上缩短接口响应时间，以及系统在并发用户增长时能否保持稳定。对应地，测试分为缓存基准测试和并发负载测试两项。
缓存基准测试由 benchmark_forum_cache.py 脚本驱动，测试对象为帖子列表接口（GET /api/forum/posts?page=0&size=10）。脚本先发起一次冷启动请求，此时缓存为空，请求穿透到 MySQL 完成查询；随后连续发起 20 次热缓存请求，此时 Redis 中已有缓存条目，请求直接命中返回。脚本分别记录两种场景下的响应时间并计算改善幅度，结果如表 5-3 所示。
表 5-3 缓存基准测试结果
| 指标 | 数值 |
|------|------|
| 冷启动响应时间 | 待填写 ms |
| 热缓存平均响应时间 | 待填写 ms |
| 热缓存最小响应时间 | 待填写 ms |
| 热缓存最大响应时间 | 待填写 ms |
| 热缓存 P95 响应时间 | 待填写 ms |
| 性能提升百分比 | 待填写 % |
缓存基准测试报告截图如图 5-2 所示。
图 5-2 缓存基准测试报告截图（待补充：需执行 benchmark_forum_cache.py 后截取）
并发负载测试由 load_test_concurrent.py 脚本驱动，底层使用 Python 的 ThreadPoolExecutor 创建线程池。测试分三轮进行，并发用户数依次为 10、50 和 100，每个虚拟用户向帖子列表接口发送 20 次 GET 请求。正式测试前脚本会先发起一次预热请求让缓存就绪，排除冷启动对后续数据的干扰。每轮记录平均响应时间、P95 响应时间、成功率和吞吐量（QPS），结果如表 5-4 所示。
表 5-4 并发负载测试结果
| 并发用户数 | 总请求数 | 成功数 | 失败数 | 成功率 | 平均响应(ms) | P95(ms) | QPS |
|:--------:|:------:|:-----:|:-----:|:-----:|:---------:|:------:|:---:|
| 10 | 200 | 待填写 | 待填写 | 待填写 | 待填写 | 待填写 | 待填写 |
| 50 | 1000 | 待填写 | 待填写 | 待填写 | 待填写 | 待填写 | 待填写 |
| 100 | 2000 | 待填写 | 待填写 | 待填写 | 待填写 | 待填写 | 待填写 |
并发负载测试报告截图如图 5-3 所示。
图 5-3 并发负载测试报告截图（待补充：需执行 load_test_concurrent.py 后截取）

5.4 测试结论
功能测试覆盖了系统全部 22 个 API 接口和 11 个端到端业务场景，通过率为 100%。正常业务流程（分区查询、注册登录、发帖评论搜索、通知和媒体浏览）运行结果与预期一致，异常路径下的四种防护机制（无令牌拦截、越权操作拦截、并发点赞去重、登出后会话失效）也按照第三章需求分析和第四章安全设计中定义的行为正确响应。
性能测试从两个角度评估了系统的运行效率。缓存基准测试表明 Redis 缓存对帖子列表接口的响应时间有明显的改善作用，并发负载测试在 10 至 100 并发用户的规模下验证了系统能够维持稳定的响应和较高的成功率。
需要指出的是，上述测试全部在本地开发环境的单机上执行，与生产环境的网络延迟和硬件配置存在差异。系统尚未进行云服务器部署下的大规模压力测试，测试覆盖的广度和深度仍有提升空间，相关局限将在第六章中进一步讨论。
