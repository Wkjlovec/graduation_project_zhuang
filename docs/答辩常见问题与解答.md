# 毕业答辩常见问题与解答

> 课题：基于微服务架构的高校论坛系统设计与实现（PC + 移动端）

---

## 一、架构与技术选型类

### Q1：为什么选择微服务架构而不是单体架构？

**回答要点：**

论文1.1节分析了现有校园论坛的问题：早期BBS单体架构（如水木清华BBS、北大未名BBS），用户规模增长后出现并发瓶颈、扩展困难、迭代缓慢。前后端耦合导致任何局部修改都需要全局重新部署。

微服务架构的优势：
- 5个业务服务+网关独立部署（auth、forum、notification、media、search + gateway），职责清晰
- 各服务通过 Nacos 注册发现，实例增减对上游透明
- 论坛服务可独立扩容应对读请求峰值
- 服务之间通过 HTTP/Feign 通信，边界明确

**补充说明**：微服务也带来了额外复杂度。论文2.1节也讨论了这一点——通信方式选择对性能有影响[18]，断路器和故障转移机制是防止故障蔓延的关键[17]。本项目通过 Nacos 统一注册发现、Gateway 统一入口、Docker Compose 一键启停来控制运维复杂度。

---

### Q2：为什么选择 Spring Cloud Alibaba + Nacos，而不是 Eureka 或 Consul？

**回答要点：**

| 维度 | Nacos | Eureka | Consul |
|------|-------|--------|--------|
| 功能 | 注册发现 + 配置管理 | 仅注册发现 | 注册发现 + KV |
| 维护状态 | 阿里活跃维护 | Netflix已停止维护 | HashiCorp维护 |
| 生态 | Spring Cloud Alibaba 原生 | Spring Cloud Netflix（已进入维护模式） | 需额外适配 |
| 部署复杂度 | 单机standalone即可 | 较简单 | 需要额外组件 |

选择理由：
1. Nacos 同时支持**注册发现和配置管理**，一个组件覆盖两类需求
2. Spring Cloud Alibaba 生态与 Spring Cloud Gateway 无缝集成
3. 社区活跃，文档丰富，问题排查资料充足
4. standalone 模式下部署简单，适合毕设开发环境

---

### Q3：为什么选择 Spring Cloud Gateway 而不是 Nginx 做网关？

**回答要点：**

Spring Cloud Gateway 是应用级网关，Nginx 是基础设施级反向代理，两者定位不同：

1. **服务发现集成**：Gateway 原生支持 `lb://服务名` 路由，新增服务无需手动修改配置；Nginx 需手动维护 upstream 列表
2. **过滤器链**：Gateway 支持自定义 Java 过滤器，本项目在 `JwtRelayFilter` 中实现了 JWT 验证、Redis 会话校验、用户身份透传，这些逻辑用 Nginx + Lua 实现成本更高
3. **与 Spring 生态深度集成**：共享 JWT 密钥配置、Redis 连接等 Spring Boot 基础设施
4. **动态路由**：配合 Nacos 可实现动态路由变更

在生产环境中，通常 Nginx 在前（处理静态资源、SSL、限流），Gateway 在后（处理业务路由、鉴权），两者互补。本项目简化了部署层，直接使用 Gateway 统一入口。

---

### Q4：为什么前端选择 Vue 3 而不是 React？

**回答要点：**

1. **组合式 API（Composition API）**：Vue 3 的 `setup` + `ref`/`reactive` 在状态管理上比 React Hooks 更直观，学习曲线更平缓
2. **双端 UI 框架生态**：Element Plus（PC）和 Vant（移动端）都是 Vue 3 原生适配的成熟组件库，且同为国内团队维护，中文文档完善
3. **模板语法**：Vue 的模板语法对初学者更友好，与传统 HTML 结构一致
4. **TypeScript 支持**：Vue 3 对 TypeScript 的支持已经非常成熟

React 也是优秀的选择，但在本项目中 Vue 3 在双端 UI 库生态和中文社区支持上更有优势。

---

### Q5：为什么使用 JWT 而不是传统 Session？

**回答要点：**

传统 Session 方案将会话信息存储在服务端内存，在微服务架构下存在问题：
- 多服务间 Session 不共享，需引入 Session 同步或集中存储
- 横向扩展时需要 sticky session 或分布式 Session

JWT 方案优势：
1. **无状态**：令牌自包含用户信息，网关可独立验证，不依赖 auth 服务
2. **跨服务透传**：网关验证后通过 `X-User-Id` / `X-Username` 头传递身份

**但本项目并非纯无状态 JWT**：
- 引入了 Redis 会话存储 (`auth:session:{sid}`)，用于支持**登出即失效**——纯 JWT 方案中令牌签发后无法撤销
- 这是 JWT + Session 的混合方案，兼顾了无状态验证的性能和会话管理的安全性
- 登出时删除 Redis 会话，网关校验 session 不存在 → 拒绝请求

---

## 二、系统设计与实现类

### Q6：系统中的缓存策略是怎么设计的？如何保证缓存一致性？

**回答要点：**

**缓存架构**：使用 Spring Cache + Redis 作为缓存层

| 缓存名 | TTL | 用途 |
|--------|-----|------|
| `forum:post:list` | 2 分钟 | 帖子列表分页缓存 |
| `forum:post:detail` | 5 分钟 | 帖子详情缓存 |
| 默认 | 10 分钟 | 其他缓存 |

**一致性策略**：
1. **主动失效**：写操作（发帖、编辑、删除、评论、点赞）通过 `@CacheEvict` 主动清除相关缓存
2. **TTL 兜底**：即使主动失效遗漏，短 TTL（2~5 分钟）保证数据最终一致
3. **序列化**：使用 `GenericJackson2JsonRedisSerializer`，支持泛型反序列化，避免类型丢失

**性能效果**：
- 冷请求（首次）：约 22~31ms
- 热请求（缓存命中）：约 4~5ms
- 提升比例：81~85%

**未采用的方案及理由**：
- 未使用缓存预热（业务场景不需要）
- 未使用延迟双删（复杂度过高，短 TTL 已足够）
- 未使用 Caffeine 本地缓存（微服务多实例时本地缓存不一致）

---

### Q7：JWT 双令牌机制具体是怎么工作的？

**回答要点：**

**双令牌设计**：
- **Access Token**：短期有效（默认 86400 秒 / 24 小时），用于接口鉴权
- **Refresh Token**：长期有效（默认 604800 秒 / 7 天），用于刷新 Access Token

**完整流程**：
1. 登录成功 → auth-service 签发 Access + Refresh Token，Redis 写入 session
2. 前端携带 Access Token → Gateway 验证签名、类型、Redis session
3. Access Token 过期 → 前端收到 401 → 自动调用 `/api/auth/refresh`
4. Refresh Token 有效 → 签发新 Access Token → 前端重试原请求
5. Refresh Token 也过期 → 清除本地存储 → 跳转登录页
6. 登出 → 调用 `/api/auth/logout` → Redis 删除 session → 旧 Token 全部失效

**前端并发刷新处理**：
- 当多个请求同时收到 401 时，只触发一次 refresh 调用
- 其他请求进入 pending queue，refresh 成功后统一重试
- 通过 `isRefreshing` 标志位 + Promise 队列实现

---

### Q8：网关的 JWT 过滤器具体做了什么？

**回答要点：**

`JwtRelayFilter` 是一个全局过滤器（order = -100，最先执行），核心逻辑：

1. **路径白名单判断**：OPTIONS 请求、`/api/auth/**`、`/api/users/ping`、`/actuator/**` 直接放行
2. **匿名可读路径**：GET 请求的帖子列表/详情、分区、首页通知、推荐、搜索等只读接口允许匿名访问
3. **JWT 验证**：
   - 提取 `Authorization: Bearer <token>`
   - 使用 JJWT 验证签名（HMAC-SHA256）
   - 检查 token 类型为 `access`（不接受 refresh token）
4. **Redis 会话校验**：
   - 从 JWT claims 中提取 `sid`（session ID）
   - 查询 Redis `auth:session:{sid}` 是否存在
   - 不存在 → 说明已登出或会话过期 → 401
5. **身份透传**：
   - 从 claims 中提取 `uid`、`username`
   - 写入请求头 `X-User-Id`、`X-Username`
   - 下游服务通过这两个头获取用户身份

---

### Q9：帖子的权限控制是怎么实现的？

**回答要点：**

权限控制分为三层：

1. **网关层（Gateway JwtRelayFilter）**：
   - 区分公开路径（列表、详情）和受保护路径（发帖、评论、点赞）
   - 受保护路径必须携带有效 JWT + 活跃 session

2. **前端路由层（Vue Router beforeEach）**：
   - `/login`、`/posts`、`/posts/:id` 为公共页面
   - 其他页面检查 localStorage 中是否有 token，无则跳转登录

3. **业务服务层（forum-service）**：
   - 发帖：需要 `X-User-Id` 头（网关注入），记录作者
   - 编辑/删除帖子：校验当前用户 ID == 帖子 author_id
   - 删除评论：允许评论作者或帖子作者删除
   - 点赞：数据库唯一约束 `(post_id, user_id)` 防止重复

---

### Q10：搜索功能是如何实现的？为什么没用 Elasticsearch？

**回答要点：**

**当前实现**：
- search-service 通过 OpenFeign 调用 forum-service 获取帖子列表
- 在内存中对标题和内容进行关键词过滤
- 支持分区过滤 + 分页

**未使用 Elasticsearch 的理由**：
1. 本科毕设范围控制，避免过度工程化
2. 当前数据规模（高校论坛）不大，内存过滤可满足需求
3. ES 引入会增加部署复杂度（需要额外的 JVM 进程、索引管理、数据同步）
4. 项目设计原则：「能用单服务落地，不额外拆服务」

**未来优化方向**：
- 数据量增大后可引入 Elasticsearch
- 实现全文搜索、高亮、权重排序
- 使用 Canal 或 MQ 做数据库到 ES 的增量同步

---

### Q11：服务间通信是怎么实现的？

**回答要点：**

本系统服务间通信使用 **HTTP + OpenFeign** 方式：

1. **search-service → forum-service**：通过 `@FeignClient(name="forum-service")` 声明式调用帖子列表接口
2. **负载均衡**：Feign 集成 Spring Cloud LoadBalancer，通过 Nacos 服务发现自动路由
3. **降级策略**：当前版本未实现 Sentinel/Hystrix 熔断降级（毕设范围取舍）

**为什么不用消息队列（MQ）**：
- 当前服务间调用是同步的（搜索需要即时返回结果）
- 通知场景可以用 MQ 解耦，但当前规模下直接调用已足够
- 未来可引入 RocketMQ/Kafka 做异步通知和事件驱动

---

## 三、测试与质量保障类

### Q12：测试策略是怎么设计的？测试数据可靠吗？

**回答要点：**

论文第五章详细描述了测试方案，采用全链路黑盒测试方法：

**测试层次**：

| 层次 | 方式 | 工具 | 覆盖内容 |
|------|------|------|----------|
| 功能测试 | 自动化脚本 | Python (run_system_regression.py) | 11场景·22接口 |
| 缓存基准 | 冷/热对比 | Python (benchmark_forum_cache.py) | 冷/热请求延迟、P95、提升比 |
| 并发负载 | 线程池 | Python (load_test_concurrent.py) | 10/50/100并发用户 |
| 手工联调 | 端到端 | 浏览器 PC+移动 | 主流程可演示 |

**测试数据可靠性**：
1. **可复现**：所有测试脚本仅使用Python标准库，一键执行 `generate-test-reports.sh`
2. **报告落盘**：每次执行生成 Markdown 报告，含时间戳和具体数值
3. **多次验证**：缓存基准默认做 20 次热请求取均值和 P95，减少偶然误差
4. **真实环境**：测试在完整微服务环境下运行（非 Mock），通过网关调用全链路

**异常场景覆盖（场景6-9）**：
- 无 Token 写操作 → 预期 code=4010
- 非作者删帖 → 预期 code=4031
- 并发重复点赞 → 预期返回码集合为 [0, 4091]
- 登出后旧 Token → 预期 code=4010

---

### Q13：缓存基准测试的数据怎么解读？80% 的提升可信吗？

**回答要点：**

**测试方法**：
1. 对帖子列表接口 `/api/forum/posts?page=0&size=10` 先做 1 次冷请求（清除缓存后首次查库）
2. 再做 30 次连续热请求（命中 Redis 缓存）
3. 计算提升比例：`(冷 - 热均值) / 冷 × 100%`

**实际数据**（两次独立测试）：

| 指标 | 测试1 | 测试2 |
|------|-------|-------|
| 冷请求 | 31.45ms | 22.48ms |
| 热均值 | 4.85ms | 4.14ms |
| P95 | 6.35ms | 4.73ms |
| 提升 | 84.59% | 81.57% |

**可信度分析**：
- 冷请求包含数据库查询 + ORM 映射 + 序列化 + 网络传输
- 热请求省去了数据库查询环节，直接从 Redis 读取序列化结果
- 80%+ 的提升符合「数据库 I/O 是主要延迟瓶颈」的预期
- P95 < 7ms 表明缓存命中后延迟非常稳定

---

## 四、工程实践与部署类

### Q14：一键启停脚本是怎么设计的？

**回答要点：**

项目提供完整的运维脚本链：

1. **基础设施启动** (`start-infra-auto.sh`)：
   - 有 Docker → 自动 `docker compose up -d` 启动 MySQL + Redis + Nacos
   - 无 Docker → 使用本机 MariaDB/Redis + 提示 Nacos 状态

2. **后端服务启动** (`start-backend-all.sh`)：
   - 先构建 common-lib（`mvn -pl common-lib -am install`）
   - 按依赖顺序启动 6 个服务：auth → forum → notification → media → search → gateway（最后）
   - 每个服务启动后自动 `/actuator/health` 验活
   - 支持三种模式：`full`（需 Nacos）、`direct`（无 Nacos，静态路由）、`auto`（自动判断）

3. **验活检查** (`verify-backend-all.sh`)：
   - 检查所有 actuator 健康端点
   - 验证网关业务路由 `/api/forum/sections`

4. **测试报告** (`generate-test-reports.sh`)：
   - 自动选择 full/direct 模式
   - 生成回归报告 + 缓存基准报告到 `docs/`

---

### Q15：如果某个微服务挂了会怎么样？有容错机制吗？

**回答要点：**

**当前实现**：
- 各服务独立进程，单服务宕机不影响其他服务运行
- 网关对不可用服务返回 503 错误
- 前端有错误提示和重试机制

**未实现的容错手段**（毕设范围取舍）：
- 未引入熔断器（Sentinel/Resilience4j）：当下游服务不可用时没有快速失败
- 未做服务多实例部署：单实例宕机即不可用
- 未配置重试策略（Gateway Retry Filter）

**设计原则**：
> 先保证端到端可运行，再做增强。本项目优先验证架构可行性和功能完整性，高可用是未来方向。

---

### Q16：为什么没有引入 CI/CD？

**回答要点：**

这是一个主动的范围取舍决策：

1. **本科毕设重点**：验证系统设计与实现能力，CI/CD 不是核心评审点
2. **替代方案**：通过本地可复现的脚本闭环（启停 + 验活 + 回归 + 报告）替代自动化流水线
3. **工程完整度**：脚本产物等价于 CI 的构建产物，区别仅在于触发方式（手动 vs 提交触发）
4. **部署环境**：毕设通常在单机演示，不需要多环境 CI/CD 流水线

如果要引入，可以用 GitHub Actions：
- push → 构建 → 单元测试 → 打包 → 部署 → 回归测试 → 报告上传

---

## 五、安全相关类

### Q17：系统有哪些安全措施？

**回答要点：**

| 安全维度 | 实现方式 |
|----------|----------|
| 认证 | JWT 双令牌 + Redis 会话校验 |
| 密码存储 | BCrypt 单向散列（不可逆） |
| 会话管理 | Redis 集中管理，登出即失效 |
| 防暴力破解 | 登录失败 N 次后锁定（IP + 用户名维度） |
| 权限控制 | 网关路径白名单 + 业务层作者校验 |
| 跨域 | Gateway 全局 CORS 配置（限定开发域名） |
| 接口规范 | 写操作必须携带有效 Token |
| 数据完整性 | 数据库唯一约束（点赞去重、用户名唯一） |

**未覆盖的安全领域**（超出毕设范围）：
- SQL 注入防护：JPA 参数化查询已内置防护
- XSS 防护：前端框架默认转义（Vue 的 `{{ }}` 自动转义 HTML）
- CSRF：API 为无状态 JWT 鉴权，已关闭 CSRF（RESTful API 场景下合理）
- WAF / 限流 / 反爬虫：未引入

---

### Q18：为什么关闭了 CSRF 防护？

**回答要点：**

CSRF（跨站请求伪造）防护主要针对基于 Cookie/Session 的认证方式。攻击者利用浏览器自动携带 Cookie 的特性，诱导用户访问恶意页面发起伪造请求。

本系统使用 **JWT Bearer Token** 认证：
1. Token 存储在 localStorage，不会被浏览器自动携带
2. 每个请求需要手动在 `Authorization` 头中携带 Token
3. 攻击者无法通过伪造页面获取到用户的 JWT

因此，关闭 CSRF 是合理的。Spring Security 官方文档也建议对无状态 RESTful API 关闭 CSRF。

---

### Q19：登录失败锁定机制是怎么实现的？

**回答要点：**

1. Redis Key：`auth:login:fail:{username}:{ip}`
2. 每次登录失败 → 计数器 +1
3. 计数器达到阈值（默认 5 次）→ 锁定（默认 600 秒）
4. 锁定期间拒绝该用户从该 IP 的登录请求
5. 登录成功 → 清除计数器
6. Redis Key 自带 TTL → 自动过期解锁

**安全考量**：
- 按 `username + IP` 双维度锁定，防止同一 IP 批量尝试不同账号
- 不按纯 IP 锁定，避免共享 IP 环境（如校园网）下误伤

---

## 六、项目特色与创新类

### Q20：你认为这个项目最大的创新点是什么？

**回答要点：**

三个核心创新点：

1. **完整的微服务工程闭环**：
   - 不仅实现了功能，还构建了从部署到测试到报告的完整链路
   - 一键启停 → 健康验活 → 自动化回归 → 缓存基准 → 报告产物
   - 这种「可复现的工程验证体系」在同类毕设中较少见

2. **PC + 移动双端协同**：
   - 两端共享同一套后端 API，统一鉴权逻辑
   - 不同 localStorage 前缀支持同域运行不冲突
   - 401 自动刷新 + 登出同步等体验一致性设计

3. **安全机制的实用性**：
   - JWT + Redis 会话混合方案，兼顾无状态性和登出可控
   - 登录失败锁定、并发点赞去重等都是实际场景需求
   - 不是为了技术展示而引入，每个安全措施都对应具体威胁

---

### Q21：论文中提到 AI 工具普及对论坛的影响，这部分和你的系统有什么关系？

**回答要点：**

这是在研究背景部分提出的问题观察：

1. **现象**：AI 工具（如 ChatGPT）普及后，用户生成内容速度显著加快，论坛中的学习笔记、代码示例、经验总结快速增长
2. **挑战**：信息密度增加但质量参差不齐，传统人工筛选难以为继
3. **与本系统的关联**：
   - 搜索功能帮助用户从大量内容中快速定位需要的信息
   - 分区浏览提供了结构化的信息组织方式
   - 缓存机制保障了高访问量下的响应性能

4. **局限性**：本系统目前不具备内容质量评估能力，这属于未来优化方向（可结合 NLP / AI 做内容评分和推荐排序）

---

## 七、项目管理与开发过程类

### Q22：开发过程中遇到的最大困难是什么？如何解决的？

**回答要点：**

**困难 1：缓存与数据库一致性**
- 问题：写操作后列表缓存可能返回旧数据
- 解决：`@CacheEvict(allEntries=true)` 在写操作时清除整个列表缓存 + 短 TTL 兜底

**困难 2：双端认证状态同步**
- 问题：多个并发请求同时收到 401 时，如何避免重复刷新 Token
- 解决：Axios 拦截器中使用 `isRefreshing` 标志位 + pending Promise 队列，只执行一次 refresh，其他请求等待后重试

**困难 3：微服务启动顺序依赖**
- 问题：网关先于业务服务启动时，Nacos 中无可用实例，路由报错
- 解决：启动脚本中固定顺序（gateway 最后启动），每个服务启动后验活才继续

**困难 4：Gateway 中 Redis 的响应式与阻塞式冲突**
- 问题：Gateway 基于 WebFlux（响应式），不能使用普通 RedisTemplate
- 解决：使用 `spring-boot-starter-data-redis-reactive` 的 ReactiveRedisTemplate

---

### Q23：系统的代码量大概有多少？开发周期是怎样的？

**回答要点：**

**代码规模**：
- 后端：6 个微服务 + 1 个公共库，涵盖控制器、服务层、实体、DTO、过滤器、配置等
- 前端：2 个独立 Vue 3 项目（PC + 移动），共计 12 个 Vue 组件、10 个 API 模块、2 个 Store
- 运维：Docker Compose + 8 个 Shell 脚本 + 3 个 Python 测试脚本

**开发路径**：
1. 基础设施搭建（Docker + Nacos + MySQL + Redis）
2. 网关 + 注册发现
3. 登录鉴权（JWT + Spring Security）
4. 论坛核心（帖子/评论/点赞）
5. PC 端页面骨架
6. 移动端页面骨架
7. 缓存优化与测试数据
8. 扩展服务（通知、搜索、推荐）
9. 自动化测试与验收

---

## 八、扩展与未来方向类

### Q24：如果要支持 10 万用户同时在线，系统需要做哪些改造？

**回答要点：**

1. **服务扩容**：Kubernetes 编排，forum-service 和 auth-service 多实例水平扩容
2. **数据库**：MySQL 读写分离（主从复制），考虑分库分表
3. **缓存**：Redis Cluster 集群，增大缓存容量和吞吐
4. **网关**：多实例 + Nginx 负载均衡前置
5. **消息队列**：引入 Kafka/RocketMQ 做通知异步化，削峰填谷
6. **搜索**：Elasticsearch 替代内存过滤
7. **CDN**：静态资源（前端 JS/CSS/图片）走 CDN 加速
8. **监控**：Prometheus + Grafana 全链路指标，SkyWalking 分布式链路追踪
9. **限流**：Sentinel 对热点接口限流保护

---

### Q25：如何保证微服务之间的数据一致性？

**回答要点：**

**当前策略**：
- 各服务共享同一个 MySQL 数据库（`forum_system`），通过 JPA 事务保证单服务内一致性
- 服务间不存在跨服务写事务场景（设计时刻意避免）

**如果需要跨服务事务**（未来场景）：
1. **最终一致性**：通过 MQ 消息补偿（如发帖成功后异步通知 notification-service）
2. **Saga 模式**：每个服务有本地事务，失败时执行补偿事务
3. **分布式事务框架**：Seata（与 Spring Cloud Alibaba 集成），但复杂度高

**为什么当前不需要**：
- 论坛场景下，用户操作天然是单服务完成的（发帖在 forum-service，登录在 auth-service）
- search-service 只读不写，不涉及事务
- 通知场景即使丢失也不影响核心功能（非关键路径）

---

### Q26：你的论文中提到"可复现测试"，这和软件工程中的概念有什么关系？

**回答要点：**

可复现测试对应软件工程中**回归测试（Regression Testing）**和**持续集成（CI）**的核心理念：

1. **确定性输入**：测试脚本注册新用户、发布特定内容、执行固定操作序列，每次运行输入一致
2. **可验证输出**：断言 HTTP 状态码和业务响应码，通过/失败有明确判定标准
3. **环境可重建**：Docker Compose 一键拉起基础设施，启动脚本重建服务
4. **报告可对比**：每次运行生成含时间戳的 Markdown 报告，可纵向对比历史数据

这与 DevOps 中「代码即基础设施」「测试即文档」的思想一致，只是触发方式从 CI 流水线改为手动脚本执行（适应毕设交付场景）。

---

## 九、数据库设计类

### Q27：为什么所有服务共享一个数据库？微服务不应该每个服务独立数据库吗？

**回答要点：**

理论上微服务推荐 Database-per-Service 模式，但在本项目中做了务实的取舍：

1. **毕设规模**：6 个服务共用一个 MySQL 实例，管理简单，资源占用低
2. **数据关联**：帖子和评论需要关联用户信息，独立数据库需要额外的同步机制
3. **运维成本**：多个数据库实例的备份、迁移、权限管理在单人毕设中不划算

**逻辑隔离**：
- 虽然共享数据库，但各服务只访问自己负责的表
- auth 只操作 `user_account`
- forum 只操作 `forum_post`、`post_comment`、`post_like`、`forum_section`
- notification 只操作 `notification_message`

**如果需要独立**：
- 可以在 Nacos 配置中为每个服务指定不同的数据源
- 使用事件驱动（MQ）代替直接数据关联

---

### Q28：帖子点赞的去重是怎么实现的？

**回答要点：**

**实现方式**：数据库唯一约束 + 业务层异常捕获

1. `post_like` 表有 `(post_id, user_id)` 联合唯一约束
2. 用户点赞时尝试 INSERT
3. 如果违反唯一约束 → 捕获 `DataIntegrityViolationException` → 返回业务错误码 4091（已点赞）
4. 同时更新 `forum_post.like_count` 字段

**为什么不用 Redis 做去重**：
- 数据库唯一约束是最可靠的去重手段，不依赖缓存可用性
- 性能可接受（点赞不是高频操作，不是秒杀场景）
- 减少一致性维护成本

**并发测试验证**：
- 回归脚本模拟两个线程同时对同一帖子点赞
- 预期结果：1 个成功（code=0），1 个业务失败（code=4091）
- 实际测试：通过

---

## 十、补充高频问题

### Q29：Gateway 的 CORS 是怎么配置的？

网关在 `application.yml` 中配置了全局 CORS：
- 允许的来源：`http://localhost:5173`（PC Vite）、`http://localhost:5174`（移动端 Vite）及 `127.0.0.1` 对应地址
- 允许的方法：GET、POST、PUT、DELETE、OPTIONS
- `allowCredentials: true`（允许携带认证信息）

生产环境需要将来源改为实际域名，或使用 Nginx 统一处理 CORS。

---

### Q30：如果答辩时系统跑不起来怎么办？

**应急方案**：
1. **提前演练 3 次以上**，确保本地环境稳定
2. **录制演示视频**作为备用
3. **准备截图**覆盖关键场景（登录、发帖、评论、搜索、通知、点赞、权限拒绝）
4. **脚本模式降级**：如果网关/Nacos 不可用，使用 `RUN_MODE=direct` 启动后端（静态路由），至少可以展示核心功能
5. **离线报告**：回归测试和缓存基准报告已生成为 Markdown 文件，可直接展示

---

## 十一、软件工程方法论类（答辩高频）

### Q31：你的论文采用了哪些与软件工程相关的研究方法？

**回答要点：**

本文是一篇**工程实践类**论文，主要采用以下研究方法：

1. **系统分析与设计方法**：第三章通过用例建模（8个功能模块的用例图）、非功能性需求量化指标和角色权限矩阵完成需求分析；第四章按分层架构设计、模块划分、数据库建模、安全机制设计逐层展开系统设计
2. **原型开发法**：按"基础设施 → 核心服务 → 前端骨架 → 扩展服务 → 测试验收"的路径逐步迭代实现
3. **黑盒测试法**：功能测试脚本按照预设业务流程向系统各接口发送标准HTTP请求，将实际返回值与预期值逐一比对
4. **对比实验法**：缓存基准测试对比冷启动和热缓存两种条件下的接口响应时间，量化缓存策略的效果
5. **文献分析法**：第一章和第二章引用了21篇国内外文献，梳理了从BBS到微服务的技术演变和当前研究不足

---

### Q32：论文中的核心概念是什么？用自己的话高度概括。

**回答要点：**

本文的核心概念是**"微服务架构"**和**"可复现的工程验证"**。

用一句话概括：将传统单体论坛拆分为多个独立部署的微服务，通过统一网关、双令牌认证和Redis缓存解决安全与性能问题，同时建立自动化测试体系确保每次变更后系统行为可验证。

核心概念的三个维度：
- **微服务拆分**：将单体应用按业务域拆解为认证、论坛、通知、搜索、媒体5个独立服务，通过Nacos注册发现和Gateway统一路由协同工作
- **安全闭环**：JWT双令牌（Access+Refresh）结合Redis会话存储，实现登录-刷新-登出的完整认证生命周期
- **可复现验证**：Python自动化测试脚本覆盖全部22个API接口和11个业务场景，每次执行产生可对比的Markdown报告

---

### Q33：选题缘由是什么？研究具有何种现实指导意义？

**回答要点：**

**选题缘由**：
- 论文1.1节分析了高校交流场景中的实际痛点：即时通讯中课程答疑几小时后就被覆盖，历史信息难以检索
- 既有校园论坛（如水木清华BBS、北大未名BBS）底层技术多年未升级，单体架构面临并发瓶颈和维护成本持续增加
- 根据CNNIC第57次统计报告，移动端用户比例已达99.6%，但多数校园论坛仅有PC端
- 论文1.2节指出：论坛技术选型已演进到微服务阶段，但在高校场景中将微服务完整应用于论坛并配套可复现测试体系的工程实践仍较少

**现实指导意义**：
1. 为高校论坛系统的技术升级提供了一套从架构到测试的完整工程参考方案
2. 验证了微服务拆分在中小规模论坛场景下的可行性和效果（缓存提升80%+，测试100%通过）
3. 一键启停和自动化测试脚本的设计思路可复用到其他微服务项目

---

### Q34：核心概念怎样在论文中体现？

**回答要点：**

以"微服务架构"为例，它在论文的六个章节中逐层体现：

| 章节 | 微服务架构的体现 |
|------|---------------|
| 第一章 | 1.1节分析单体架构痛点 → 引出微服务需求；1.2节梳理微服务技术演进 |
| 第二章 | 2.1节介绍Spring Cloud Alibaba/Nacos/Gateway/Feign的原理 |
| 第三章 | 3.3节可扩展性需求明确要求"新模块注册Nacos即可接入" |
| 第四章 | 4.1节四层架构设计；4.2节7个Maven子模块划分；4.4节网关统一鉴权 |
| 第五章 | 功能测试覆盖全部22个分布在5个服务中的API接口 |
| 第六章 | 6.2节指出搜索服务和部署架构的不足，是微服务进一步优化的方向 |

同样，"可复现测试"从第三章非功能需求（可维护性）→ 第四章运维脚本设计 → 第五章三个Python测试脚本实际执行 → 第六章测试体系待完善，形成完整链路。

---

### Q35：从反面思考——如果不用微服务架构，结果会怎样？

**回答要点：**

如果采用单体架构：

1. **开发效率下降**：认证逻辑修改可能影响论坛模块，所有人在同一代码库中工作容易冲突
2. **无法独立扩容**：帖子列表是高频读接口，单体架构下只能整体扩容，资源浪费
3. **部署风险高**：任何微小修改都需要全局重新部署，一个Bug可能导致整个系统不可用
4. **技术栈受限**：无法为搜索服务选择更合适的检索方案（未来可替换为Elasticsearch而不影响其他服务）

**但微服务也有代价**：
- 增加了网络通信复杂度（服务间调用延迟）
- 需要额外的基础设施（Nacos、Gateway）
- 分布式环境下的故障排查更困难

论文中的做法是：在保持微服务架构优势的同时，通过共享数据库、Docker Compose一键部署、自动化测试等手段控制复杂度，在本科毕设范围内取得平衡。

---

### Q36：论文的理论基础是什么？与主体框架有何关联？

**回答要点：**

本文的理论基础主要来自**软件工程领域**，集中体现在第二章：

1. **微服务架构理论**：将单体应用拆分为独立服务，每个服务拥有自己的数据存储和部署周期[12]。这是第四章架构设计的直接依据
2. **Spring框架的IoC和AOP机制**：控制反转降低模块耦合，面向切面编程统一处理横切关注点[11]。这是后端代码组织方式的基础
3. **JWT无状态认证理论**：Aldea等人的研究[7]指出短生命周期令牌和最小权限原则可缩小攻击面，这是第四章安全设计的理论依据
4. **关系数据库范式理论**：6张表满足3NF，冗余字段的取舍基于查询性能与数据一致性的权衡分析
5. **缓存理论**：Cache-Aside模式（先查缓存，未命中查库写缓存，写操作清缓存），是帖子缓存策略的理论来源

理论基础与主体框架的关联：第二章的技术原理 → 第三章转化为具体的需求指标 → 第四章在设计和实现中落地 → 第五章通过测试验证理论预期（如缓存提升比例）。

---

### Q37：经过你的研究，结果是怎样的？有何正面或负面效果？

**回答要点：**

**正面效果（第五章测试数据验证）**：
- 功能测试11个场景22个接口全部通过，系统功能完整且防护机制有效
- Redis缓存使帖子列表响应时间从22~31ms降至4~5ms，提升81~85%
- 并发负载测试在10~100用户下系统保持稳定
- 一键启停+自动化测试的运维体系可重复执行

**负面效果/局限（第六章6.2节）**：
- 搜索功能基于内存遍历，数据量增大后效率会明显下降
- 微服务拆分引入了额外的运维复杂度（Nacos、Gateway、多进程管理）
- 全部测试在本地单机完成，无法体现分布式部署的真实性能
- 安全测试停留在接口级别，未做SQL注入/XSS等渗透测试

---

### Q38：你的论文基于何种研究视角？

**回答要点：**

本文基于**软件工程视角**，具体是**系统设计与实现**方向。

不同于管理学视角（关注组织效率）或教育学视角（关注教学效果），本文关注的核心问题是：

1. **架构层面**：如何将单体论坛拆分为可独立部署的微服务，并通过网关和注册中心协同
2. **工程层面**：如何在同一工程中实现从需求分析到自动化测试的完整链路
3. **技术层面**：如何解决JWT认证闭环、缓存一致性、并发去重等具体工程问题

研究视角决定了论文的论证方式：不是通过问卷调查或用户访谈得出结论，而是通过**系统实现+自动化测试数据**来验证设计方案的有效性。

---

### Q39：研究对象是什么？是点的研究还是面的研究？

**回答要点：**

**研究对象**：高校场景下的论坛系统。不是研究某个具体的高校论坛产品，而是研究"如何用微服务架构设计和实现一个论坛系统"这个工程问题。

**点面关系**：
- 从"面"的角度：论文覆盖了论坛系统的完整生命周期——从需求分析（第三章）到架构设计（第四章前半）到编码实现（第四章后半）到测试验收（第五章）
- 从"点"的角度：在每个环节中聚焦关键技术点深入展开，如JWT双令牌机制、缓存读写策略、并发点赞去重、前端401静默刷新等

这是一个**以面带点**的研究——以完整系统为载体，在关键节点上做深度技术验证。

---

### Q40：研究的应然、实然、使然分别是什么？

**回答要点：**

- **应然（应该怎样）**：高校论坛系统应当具备微服务架构的松耦合性、PC+移动双端适配、完整的认证安全闭环，以及可复现的自动化测试体系（第三章需求分析）
- **实然（实际怎样）**：现有校园论坛多为单体架构，前后端耦合，缺少移动端支持，功能验证依赖手工操作，交付质量缺少量化判据（第一章1.1节现状分析）
- **使然（如何从实然到应然）**：通过Spring Cloud微服务拆分解决架构耦合，通过Vue3双端开发解决移动适配，通过JWT+Redis解决安全认证，通过Python脚本解决测试可复现（第四章设计与实现）

论文的结构正好对应了这三个层面：第一章"实然"（问题在哪）→ 第三章"应然"（目标是什么）→ 第四章"使然"（怎么实现）→ 第五章验证"使然"是否达到了"应然"。

---

### Q41：论文的结论和方案是否具有可行性和操作性？

**回答要点：**

**可行性证据**：
1. 系统已完整实现并可运行——6个后端微服务、2个前端项目、一键启停脚本全部可用
2. 功能测试11个场景22个接口100%通过——不是理论方案，是已验证的工程产物
3. 性能数据可复现——缓存基准测试脚本可随时重跑，两次独立测试结果一致（81~85%提升）

**操作性证据**：
1. Docker Compose一条命令拉起全部基础设施
2. Bash脚本一键启停6个后端服务
3. Python测试脚本一键生成测试报告
4. README文档提供了从零到可联调的完整步骤

**局限**：可行性验证仅在本地单机完成，生产环境的可行性需要云端部署进一步验证（已列为6.2节未来方向）。

---

### Q42：全文的基本框架和结构是如何安排的？

**回答要点：**

全文六章，按照"**提出问题 → 技术准备 → 分析问题 → 解决问题 → 验证方案 → 总结评价**"的逻辑组织：

| 章节 | 内容 | 逻辑角色 |
|------|------|---------|
| 第一章 绪论 | 研究背景、现状分析、研究内容 | 提出问题（为什么做） |
| 第二章 相关技术 | 后端技术（Spring Cloud/JWT/Redis/Docker）和前端技术（Vue/Element Plus/Vant） | 技术准备 |
| 第三章 需求分析 | 8个功能模块用例、5维非功能需求、角色权限矩阵 | 分析问题（做什么） |
| 第四章 设计与实现 | 架构设计→模块设计→数据库→安全→认证→论坛→前端→运维 | 解决问题（怎么做） |
| 第五章 系统测试 | 测试环境、功能测试、性能测试 | 验证方案（做得怎么样） |
| 第六章 总结展望 | 成果回顾、问题分析、未来方向 | 总结评价 |

第四章是全文最长的章节（占全文约40%），这符合系统设计类论文的特点。

---

### Q43：全文各部分之间的逻辑关系如何？

**回答要点：**

各章之间存在严格的**前因后果**关系：

- 第一章提出四个痛点（架构耦合/双端缺失/安全不足/测试不可控）→ 第三章将其转化为8个功能模块和5维非功能需求
- 第二章介绍技术选型依据 → 第四章在设计和实现中使用这些技术
- 第三章需求分析中的可扩展性需求 → 第四章的Nacos注册发现+服务独立部署架构
- 第三章性能需求（缓存命中后毫秒级）→ 第四章Redis缓存策略 → 第五章缓存基准测试验证达标
- 第三章安全需求（登录失败锁定/登出即失效）→ 第四章JWT+Redis+锁定实现 → 第五章场景6-9异常路径测试验证
- 第五章测试暴露的局限 → 第六章作为未来改进方向

**关键链路**：需求 → 设计 → 实现 → 测试 → 总结，每一环都有前一环的依据和后一环的验证。

---

### Q44：研究过程中发现了哪些不同见解？如何处理的？

**回答要点：**

**见解1：微服务是否必要？**
- 有观点认为本科毕设规模小，单体架构足够
- 我的处理：论文1.1节分析了单体架构在用户增长后的问题，同时在4.1节承认"当前业务规模尚不需要拆库"，说明微服务在当前阶段是"架构预备"而非"必须"，但验证其可行性本身就是研究价值

**见解2：JWT vs Session 之争**
- 纯JWT无法主动撤销令牌（登出失效问题）；纯Session在微服务间不共享
- 我的处理：采用混合方案——JWT签发+Redis存储会话状态，兼顾两者优势（论文2.1节和4.4节）

**见解3：数据库共享vs独立**
- 微服务理论推荐Database-per-Service，避免隐式耦合
- 我的处理：当前共享一个MySQL实例但做逻辑隔离（各服务只访问自己的表），在论文4.1节明确说明这是"在当前业务体量下的折中方案"

---

### Q45：论文虽未论及，但与其较密切相关的问题还有哪些？

**回答要点：**

1. **消息队列与事件驱动**：当前服务间用同步HTTP调用，引入RocketMQ/Kafka可实现异步通知、削峰填谷，但会增加架构复杂度
2. **全文搜索引擎**：Elasticsearch可替代当前的内存过滤方案，但需要解决数据同步（Canal/MQ）和索引管理
3. **内容审核与质量评估**：论坛内容的垃圾信息过滤、AI生成内容的标识等，需要NLP/ML能力
4. **多租户与权限细化**：当前是扁平的注册用户模型，如果区分学生/教师/管理员角色需要引入RBAC
5. **国际化（i18n）**：当前界面仅中文，多语言支持需要前后端协同改造
6. **WebSocket实时通信**：当前通知需要用户主动刷新，引入WebSocket可实现实时推送

---

### Q46：还有哪些问题自己还没有搞清楚，论述得不够透彻？

**回答要点：**（坦诚回答，展示学术态度）

1. **分布式事务**：论文4.1节提到各服务共享数据库避免了跨服务事务问题，但没有深入探讨如果拆库后如何处理（Saga、Seata等方案只在Q&A中简要提及）
2. **缓存击穿和雪崩**：论文中的缓存策略只讨论了TTL+主动淘汰，没有涉及热点key击穿、大量key同时过期等极端场景的防护
3. **微服务的粒度划分**：当前5个服务的划分主要参考业务域，但没有量化论证为什么是5个而不是3个或8个
4. **性能测试的统计严谨性**：并发负载测试使用Python标准库的ThreadPoolExecutor，受GIL限制不能真正模拟CPU密集型并发，更专业的方案应使用JMeter或Locust

---

### Q47：写作论文时立论的主要依据是什么？

**回答要点：**

立论建立在三方面依据之上：

1. **现实需求依据**：
   - CNNIC第57次报告——移动端用户99.6%，论证双端适配的必要性[4]
   - 水木清华BBS关站、北大未名BBS收缩——论证传统架构不可持续[2]

2. **技术可行性依据**：
   - Spring Cloud体系已形成完整技术栈[6]——后端微服务技术成熟
   - Aldea等人JWT安全研究[7]——认证方案有学术支撑
   - 高校微服务实践案例[8][9]——高校信息化场景已有微服务成功经验

3. **工程验证依据**：
   - 11个场景22个接口100%通过——功能正确性
   - 缓存基准两次独立测试81~85%提升——性能有效性
   - 并发负载10~100用户稳定——承载能力

立论逻辑：**有现实需求（痛点存在）+ 技术可行（方案成熟）+ 验证通过（测试达标）**→ 方案有效。

---

### Q48：论文的研究框架是否符合规范？

**回答要点：**

本文的研究框架遵循软件工程类本科论文的标准结构：

1. **符合规范的方面**：
   - 六章结构完整（绪论→技术→需求→设计实现→测试→总结）
   - 21篇参考文献，正文中15处引用，分布在第一、二、六章
   - 第三章需求分析使用了UML用例图和活动图
   - 第四章设计与实现配有流程图、ER图、接口汇总表
   - 第五章测试有明确的环境说明、用例表格和数据表格

2. **与"写书"的区别**：
   - 不是技术手册式的逐行代码解释，而是聚焦设计决策和关键实现
   - 每个设计选择都有依据（为什么用微服务而不是单体、为什么冗余字段而不是JOIN）
   - 第五章不只是"跑通了"，而是用量化数据验证设计方案的有效性
