server:
  port: ${GATEWAY_PORT:8080}

spring:
  application:
    name: gateway-service
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_SERVER_ADDR:127.0.0.1:8848}
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
      routes:
        - id: auth-route
          uri: lb://auth-user-service
          predicates:
            - Path=/api/auth/**
          filters:
            - StripPrefix=1
        - id: auth-user-route
          uri: lb://auth-user-service
          predicates:
            - Path=/api/users/**
          filters:
            - StripPrefix=1

security:
  jwt:
    secret: ${JWT_SECRET:REPLACE_WITH_AT_LEAST_32_CHAR_SECRET_KEY}

management:
  endpoints:
    web:
      exposure:
        include: health,info,gateway
