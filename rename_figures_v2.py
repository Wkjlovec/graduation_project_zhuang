# -*- coding: utf-8 -*-
"""
为论文修改.docx 中的全部图表标题重新命名（共 30 张图 + 7 张表）。

设计原则：
1. 每条标题同时指出内容与特色；
2. 结构差异化：不同标题使用不同的句法、连词、动词与切入角度，
   避免"A 与 B XX"、"...：列举"、"...用例"等反复出现；
3. 对既有标题的事实错误与错别字进行纠正：
   - 图 4-17 "跃服务" → "跨服务"（错别字）；
   - 图 5-3 / 表 5-4 "五档 / 10–1000 用户" → 实际 3 档（10/50/100）。
"""
from docx import Document
from docx.shared import Pt
from docx.oxml.ns import qn

FONT_SONG = "宋体"
FONT_TNR = "Times New Roman"


# 旧标题 -> 新标题（覆盖此前 rename_figures.py 产出的版本）
RENAMES = {
    # ========== 第三章 图 ==========
    "图 3-1 论坛系统四层架构示意":
        "图 3-1 自前端双入口向下贯穿网关、五个微服务至 MySQL 与 Redis 的总体架构",

    "图 3-2 注册、登录与会话管理用例":
        "图 3-2 游客与注册用户共享登录入口：凭据校验作为 include 环节，连败五次后由账户锁定 extend 兜底",

    "图 3-3 帖子发布、浏览与编辑操作用例":
        "图 3-3 帖子功能沿读/写边界分裂——游客止步于浏览和详情，注册用户则由发布派生编辑、删除两类 extend",

    "图 3-4 二级评论与回复权限用例":
        "图 3-4 评论与回复共用身份校验的 include 关系，作者身份成为编辑删除扩展的前提",

    "图 3-5 公告推送与已读标记用例":
        "图 3-5 通知首页摘要对匿名开放，个人通知列表需认证后方可扩展标记已读",

    "图 3-6 关键词检索与跨服务调用用例":
        "图 3-6 关键词检索用一条 include 把调用打向论坛服务取数据，分区筛选与分页浏览各自作为可选 extend 挂接",

    "图 3-7 音乐与书籍推荐浏览用例":
        "图 3-7 媒体推荐由静态预置数据支撑游客与注册用户浏览首页摘要及按类别筛选",

    "图 3-8 网关路由与 JWT 鉴权用例":
        "图 3-8 面向网关的两类流量：公开接口直达路由转发，受保护接口须依次穿过 JWT 校验、Redis 会话与身份透传三道 include",

    "图 3-9 游客与注册用户全功能用例总览":
        "图 3-9 由六项游客公开操作与八项注册用户写操作汇成的系统整体用例",

    "图 3-10 两种角色从首页到完成操作的决策流程":
        "图 3-10 一张活动图沿\u201c加载—读判定—写鉴权—刷新或登录引导\u201d主干展开所有异常分支",

    # ========== 第四章 图 ==========
    "图 4-1 微服务分层部署与基础设施编排":
        "图 4-1 四层部署结构中前端经网关接入五个服务模块，Nacos/MySQL/Redis 由 Docker Compose 统一编排",

    "图 4-2 Gateway 请求路由分发与 Nacos 服务解析":
        "图 4-2 一次前端请求如何通过前缀剥离规则分流至五个后端并由 Nacos 定位实例",

    "图 4-3 七个 Maven 子模块的依赖与调用关系":
        "图 4-3 Maven 多模块工程中六个后端服务共享 common-lib，search 通过 OpenFeign 反向依赖 forum",

    "图 4-4 六张业务表的实体关联图（Chen 记法）":
        "图 4-4 Chen 记法下五个实体经七种语义关系串接，M:N 的点赞关联在物理层对应 post_like 表",

    "图 4-5 用户提交凭据到双令牌签发的完整路径":
        "图 4-5 登录流程兼顾正常签发与五次失败触发 600 秒锁定的双通道处理",

    "图 4-6 JwtRelayFilter 七级访问控制决策链":
        "图 4-6 JwtRelayFilter 逐级下钻，三条放行分支先于四道鉴权校验执行",

    "图 4-7 响应拦截器处理 401 与并发队列的刷新逻辑":
        "图 4-7 前端在 401 响应下借助互斥标志把并发请求排入队列再批量重试",

    "图 4-8 createPost 从身份校验到缓存淘汰的执行步骤":
        "图 4-8 发帖服务端五步执行链从身份核查走到分区存在性判定、落库与列表缓存淘汰",

    "图 4-9 Redis 读缓存与写淘汰的双路径策略":
        "图 4-9 读写分离的缓存策略中读侧采用 Cache-Aside，写侧同步淘汰列表与详情两份缓存",

    "图 4-10 一级评论与二级回复的 parentCommentId 分支处理":
        "图 4-10 评论接口依据 parentCommentId 字段取值在一级评论与二级回复两条支路间分流",

    "图 4-11 桌面端首页：帖子列表、分区筛选与侧边栏推荐":
        "图 4-11 宽屏 PC 布局采用三栏切分，中央流显示帖子列表，左侧定位分区，右侧挂载媒体推荐",

    "图 4-12 移动端首页：纵向堆叠布局与底部导航":
        "图 4-12 在移动端首页上，帖子流纵向瀑布展开，屏幕底部常驻四入口 Tab 栏",

    "图 4-13 帖子详情、评论区与点赞交互界面":
        "图 4-13 帖子详情一屏同时容纳正文阅读区、带折叠控件的二级评论与可高亮的点赞按钮",

    "图 4-14 登录与注册表单页面":
        "图 4-14 登录与注册入口将表单区对称分布于页面两侧，字段包含账号、密码与确认项",

    "图 4-15 发帖编辑器：标题、分区选择与正文填写":
        "图 4-15 发帖编辑器自顶向下依次排列标题输入框、分区下拉菜单与正文填写区",

    "图 4-16 通知列表已读标记与个人中心信息展示":
        "图 4-16 通知列表与个人中心并置呈现，左图为带红点的已读标记，右图为账户资料卡",

    "图 4-17 ForumClient 跃服务调用与内存过滤的数据流转":
        "图 4-17 搜索请求经 ForumClient 远端拉取帖子，在内存中完成多字段匹配后回传前端",

    # ========== 第五章 图 ==========
    "图 5-1 11 个场景全通过的功能回归测试报告":
        "图 5-1 run_system_regression.py 输出的回归结果，11 个场景全部标记 PASS",

    "图 5-2 冷启动与热缓存响应时间对比":
        "图 5-2 冷启动与二十次热缓存耗时并列呈现，数据来自 benchmark_forum_cache.py",

    "图 5-3 五档并发（10\u20131000 用户）下的 QPS 与 P95 延迟分布":
        "图 5-3 三档压测（10/50/100 并发）下 load_test_concurrent.py 给出的延迟与 QPS 分布",

    # ========== 表 ==========
    "表 3-1 两种角色在各功能模块上的操作权限差异":
        "表 3-1 哪些功能模块允许游客直接使用、哪些必须登录后才能操作",

    "表 4-1 全部 22 个 RESTful 接口的方法、路径与认证要求":
        "表 4-1 五个后端服务向外暴露的 22 条 RESTful 接口，逐行标注 HTTP 方法、路径与是否需要认证",

    "表 4-2 各表主键、唯一索引与联合约束一览":
        "表 4-2 数据库现存索引从聚簇、唯一到联合三种形态，其创建来源横跨自增主键、JPA 注解与 @UniqueConstraint",

    "表 5-1 11 个端到端场景的操作描述、预期与实际结果":
        "表 5-1 11 条端到端用例的流水账：正常流程、异常路径与令牌机制三组场景的实测对照",

    "表 5-2 五个服务模块的 API 覆盖率汇总":
        "表 5-2 22 个 API 接口依服务模块归入五组后的覆盖统计（接口数 × 验证脚本/手工）",

    "表 5-3 冷启动与 20 次热缓存请求的响应时间对比":
        "表 5-3 帖子列表接口冷热两次请求耗时的差值与改善百分比——缓存效果的量化读数",

    "表 5-4 10\u20131000 并发用户下的平均响应、P95 与吞吐量":
        "表 5-4 三档并发（10/50/100 用户）压测结果：请求总数、成功率、平均响应、P95 与 QPS 一次列齐",
}


def _apply_font(run):
    run.font.size = Pt(10.5)
    run.font.name = FONT_TNR
    rPr = run._element.find(qn("w:rPr"))
    if rPr is None:
        rPr = run._element.makeelement(qn("w:rPr"), {})
        run._element.insert(0, rPr)
    rFonts = rPr.find(qn("w:rFonts"))
    if rFonts is None:
        rFonts = rPr.makeelement(qn("w:rFonts"), {})
        rPr.insert(0, rFonts)
    rFonts.set(qn("w:ascii"), FONT_TNR)
    rFonts.set(qn("w:hAnsi"), FONT_TNR)
    rFonts.set(qn("w:eastAsia"), FONT_SONG)


def main():
    doc_path = "docs/论文修改.docx"
    doc = Document(doc_path)

    count = 0
    for i, p in enumerate(doc.paragraphs):
        t = p.text.strip()
        if t in RENAMES:
            new_name = RENAMES[t]
            p.clear()
            run = p.add_run(new_name)
            _apply_font(run)
            count += 1
            print(f"  [{i:4d}] → {new_name}")

    doc.save(doc_path)
    print(f"\n成功重命名 {count}/{len(RENAMES)} 条标题。")


if __name__ == "__main__":
    main()
